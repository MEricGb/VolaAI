# WhatsApp business number in E.164 format (no + or spaces), e.g. 40712345678
VITE_WHATSAPP_NUMBER=14155238886

# Backend API base URL (only needed for production — dev uses the Vite proxy)
VITE_API_URL=http://localhost:3000
